# 2A202600865-LuongDinhBut-Day19

Initial repository setup.

## Git basics

Check changed files:

```bash
git status
```

Save a version locally:

```bash
git add .
git commit -m "Initial commit"
```

Send commits to GitHub:

```bash
git push origin main
```
